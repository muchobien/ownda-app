import type { Screen } from '@app/types';
import { Text } from '@app/components';
import { Container } from '../styles';

export const Category: Screen<'Onboarding'> = () => (
  <Container>
    <Text>Category</Text>
  </Container>
);
